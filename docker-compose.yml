services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: activity_planner
      POSTGRES_USER: activity_planner
      POSTGRES_PASSWORD: activity_planner
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
